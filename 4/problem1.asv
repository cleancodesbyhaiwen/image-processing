x1 = -10:0.001:10;
y1 = (0.3./(0.25*sqrt(2*pi)))*exp(-x.^2./(2*0.25.^2));

%plot(x1,y1);

x2 = -10:0.001:10;
y2 = (0.7./(0.25*sqrt(2*pi)))*exp(-(x-3).^2./(2*0.25.^2));

y3 = y1 + y2;

plot(x2,y3);

r = zeros(1,10000);

for i=1:10000
    r(i) = unifrnd(-10,10);
end

p  =zeros(1,10000);

for i=1:10000
    p(i)